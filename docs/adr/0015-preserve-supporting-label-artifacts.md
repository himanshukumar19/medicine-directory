# Preserve Supporting Label Artifacts

Fields such as `package_label_principal_display_panel`, `spl_product_data_elements`, and `spl_unclassified_section` remain available as Supporting Label Artifacts but do not replace core label sections. `questions` and `storage_and_handling` may be useful source sections, while flattened SPL data is not treated as canonical structured content.
