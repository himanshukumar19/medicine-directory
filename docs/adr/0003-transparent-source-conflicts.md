# Render Source Conflicts Transparently

The directory will display openFDA fields as supplied, even when metadata conflicts with label text, such as a `TOPICAL` route paired with tablet dosage instructions. It is a transparent source viewer rather than a clinical validator, so it must not silently repair, infer, or reject records based on domain assumptions.
