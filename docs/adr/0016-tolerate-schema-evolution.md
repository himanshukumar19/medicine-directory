# Tolerate Schema Evolution

Fields outside the currently understood openFDA shape are treated as Unmodeled Source Fields and do not invalidate a Product or Result Set. This is distinct from a Malformed Response, where the envelope or required collection structure cannot be interpreted at all.
