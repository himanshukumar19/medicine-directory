# Preserve Source Field Boundaries

Repeated or overlapping content across fields such as `warnings`, `do_not_use`, `ask_doctor`, and `stop_use` remains attached to its original Source Field Boundary. Deduplication or summarization, if introduced later, must be a derived view and must not alter the raw Product record.
