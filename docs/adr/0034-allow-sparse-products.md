# Allow Sparse Products

`set_id` is the only hard identity requirement for a user-facing Product. A record with absent or sparse `openfda` metadata remains a Sparse Product; missing brand, manufacturer, route, or label sections are represented as unavailable rather than causing the record to be rejected.
