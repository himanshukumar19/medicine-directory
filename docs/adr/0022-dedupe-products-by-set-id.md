# Deduplicate Products by Set ID

Search results are deduplicated by `set_id`, so repeated occurrences do not become separate Product results. Multiple label documents sharing a Product identity remain a provenance-level revision concern rather than additional Products.
