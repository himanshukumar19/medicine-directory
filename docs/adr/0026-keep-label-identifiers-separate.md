# Keep Label Identifiers Separate

Top-level `id` and `openfda.spl_id` are preserved as independent Label Document Identifiers. Their observed equality is not elevated into a domain invariant; `set_id` remains the Product identity while these fields provide separate provenance.
